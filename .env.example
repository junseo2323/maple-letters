# Vercel Postgres connection (auto-injected by Vercel)
# For local dev: copy to .env.local and fill in
POSTGRES_URL=

# Salt for hashing the owner password (change to a long random string in production)
OWNER_SALT=maple-letters-v1
